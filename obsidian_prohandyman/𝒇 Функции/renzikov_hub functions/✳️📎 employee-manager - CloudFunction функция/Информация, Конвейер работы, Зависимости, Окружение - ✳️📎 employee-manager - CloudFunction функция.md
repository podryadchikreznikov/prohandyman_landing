
Идентификатор - d4er1pebqu5om94082c3
Описание - 🪪 Менеджер сотрудников: создание, редактирование, удаление и получение информации о сотрудниках фирмы.
Точка входа - index.handler
Таймаут - 15 сек

---

На входе:
	-> `user_id` передаётся в контексте запроса после проверки JWT авторизатором auth-gate (на уровне API Gateway).
	-> `action` (string, **обязательно**): Передаётся через `requestContext.apiGateway.operationContext.action`. Допустимые значения:
		- `GET`: Получить информацию о сотруднике(ах) фирмы.
		- `CREATE`: Добавить нового сотрудника в фирму.
		- `EDIT`: Изменить роли сотрудника (добавить или удалить роль).
		- `DELETE`: Удалить сотрудника из фирмы.
	-> Тело запроса (для CREATE, EDIT, DELETE):
		- `firm_id` (string, **обязательно**): ID фирмы.
		- **Для `action: CREATE`**:
			- `email` (string, **обязательно**): Email пользователя для добавления.
			- `roles` (array of strings, необязательно): Список ролей. По умолчанию `["EMPLOYEE"]`.
		- **Для `action: EDIT`**:
			- `user_id_to_edit` (string, **обязательно**): ID пользователя для редактирования.
			- `role` (string, **обязательно**): Роль для изменения (только `ADMIN` или `EMPLOYEE`).
			- `sub_action` (string, **обязательно**): `ADD_ROLE` или `REMOVE_ROLE`.
		- **Для `action: DELETE`**:
			- `user_id_to_delete` (string, **обязательно**): ID пользователя для удаления.
		- **Для `action: GET`**:
			- `firm_id` (string, **обязательно**): Передаётся через query параметр.
			- `user_id_to_get` (string, необязательно): ID конкретного пользователя. Если не указан, возвращается список всех сотрудников фирмы.

Внутренняя работа:
	-> Авторизация и извлечение user_id:
		-> Извлекается `user_id` из контекста авторизатора API Gateway (`requestContext.authorizer.user_payload`). JWT проверка выполняется централизованно функцией [[🛡️ auth-gate - CloudFunction функция]] до вызова этой функции.
	-> Подробное логирование:
		-> Вывод в лог полного содержимого event и context для отладки.
	-> Получение action из requestContext.apiGateway.operationContext.
	-> Парсинг firm_id и данных запроса:
		-> Для GET: firm_id из query параметров.
		-> Для остальных: firm_id и данные из тела запроса.
	-> Создание подключений к YDB:
		-> Пул для firms-database (таблица Users).
		-> Пул для auth-data (только для CREATE, для поиска пользователя по email).
	-> Маршрутизация по action в транзакции:
		-> Для GET:
			-> Если указан user_id_to_get: выборка одного пользователя с полной информацией.
			-> Иначе: выборка всех сотрудников фирмы.
		-> Для CREATE:
			-> Поиск пользователя по email в базе auth-data.
			-> Проверка прав текущего пользователя (должен быть ADMIN или OWNER).
			-> Проверка, что пользователь ещё не является членом фирмы.
			-> Вставка нового сотрудника в таблицу Users фирмы.
		-> Для EDIT:
			-> Проверка, что пользователь не редактирует свои собственные роли.
			-> Проверка, что роль редактируемая (ADMIN или EMPLOYEE).
			-> Получение данных текущего пользователя и целевого пользователя.
			-> Проверка иерархии ролей (роль текущего пользователя должна быть выше).
			-> Проверка, что целевой пользователь не является OWNER.
			-> Добавление или удаление роли.
		-> Для DELETE:
			-> Проверка, что пользователь не удаляет сам себя.
			-> Получение данных текущего пользователя и целевого пользователя.
			-> Проверка, что целевой пользователь не является OWNER.
			-> Проверка иерархии ролей.
			-> Физическое удаление пользователя из таблицы Users фирмы.
	-> Обработка ошибок:
		-> AuthError: 403 Forbidden.
		-> LogicError: 400 Bad Request.
		-> NotFoundError: 404 Not Found.
		-> Общие исключения: 500 Internal Server Error.

На выходе:
	-> `200 OK` (GET, список): `{"data": [array of employees]}` с информацией о сотрудниках.
	-> `200 OK` (GET, один сотрудник): `{"data": {employee_object}}` с полной информацией.
	-> `201 Created` (CREATE): `{"message": "Employee added successfully", "user_id": "..."}`
	-> `200 OK` (EDIT): `{"message": "Role '...' successfully added/removed."}`
	-> `200 OK` (DELETE): `{"message": "Employee successfully deleted."}`
	-> `400 Bad Request` для логических ошибок (например, invalid action, missing params, role conflicts).
	-> `403 Forbidden` для ошибок авторизации или недостаточных прав.
	-> `404 Not Found` для несуществующих пользователей или фирм.
	-> `500 Internal Server Error` для внутренних ошибок.

---
#### Зависимости и окружение
- **Необходимые утилиты**: Использует современные утилиты из `utils/` (parse_event, get_session_pool), `utils/util_yc_sa/*`
- **Авторизация**: Выполняется централизованно через функцию [[🛡️ auth-gate - CloudFunction функция]] на уровне API Gateway
- **Переменные окружения**:
	- `YDB_ENDPOINT_FIRMS` - Эндпоинт для `firms-database`.
	- `YDB_DATABASE_FIRMS` - Путь к `firms-database`.
	- `YDB_ENDPOINT` - Эндпоинт для `auth-data` (используется только для CREATE).
	- `YDB_DATABASE` - Путь к `auth-data` (используется только для CREATE).
	- `YC_LOCKBOX_SECRET_ID` - secret_id Lockbox с authorized key JSON
	- `YC_LOCKBOX_VERSION_ID` - опционально, версия секрета
	- `YC_LOCKBOX_KEY_FIELD` - опционально, имя поля в секрете (по умолчанию key.json)
	- `STATIC_ACCESS_KEY_ID` [[🗝️ auth-service-acc - Статический ключ доступа.md]]
	- `STATIC_SECRET_ACCESS_KEY` [[🗝️ auth-service-acc - Статический ключ доступа.md]]

---
#### Структура модулей
- `index.py` - Главный обработчик с роутером действий
- `get.py` - Получение информации о сотрудниках
- `create.py` - Добавление нового сотрудника
- `edit.py` - Редактирование ролей сотрудника
- `delete.py` - Удаление сотрудника
- `custom_errors.py` - Кастомные исключения
- `utils/` - Общие утилиты (YDB, парсинг, логирование и т.д.)

---
#### Иерархия ролей
- `OWNER` (вес 3): Владелец фирмы, не может быть удалён или отредактирован.
- `ADMIN` (вес 2): Администратор, может управлять сотрудниками с меньшими правами.
- `EMPLOYEE` (вес 1): Обычный сотрудник.

Правило: Пользователь может управлять только теми сотрудниками, чья наивысшая роль ниже его собственной.