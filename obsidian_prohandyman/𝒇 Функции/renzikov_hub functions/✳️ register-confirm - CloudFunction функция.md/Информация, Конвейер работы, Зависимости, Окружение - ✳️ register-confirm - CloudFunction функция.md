
Идентификатор - d4e7ojdqgpcirm5mjmca
Описание - Подтвердить регистрацию с помощью кода, активировать пользователя и связанные приглашения.

Точка входа - index.handler
Таймаут - 15 сек

---

На входе:
	-> `email` (string, **обязателен если нет phone_number**): Email пользователя.
	-> `phone_number` (string, **обязателен если нет email**): Номер телефона пользователя.
	-> `code` (string, **обязательно**): 6-значный код подтверждения из email или SMS.

Внутренняя работа:
	-> **Парсинг запроса**: Использование `parse_event` для извлечения `email`, `phone_number` и `code` из тела запроса.
	-> **Валидация входных данных**: 
		-> Проверка наличия `code`; если отсутствует — возврат `400 Bad Request`.
		-> Проверка наличия хотя бы одного из `email` или `phone_number`; если оба отсутствуют — возврат `400 Bad Request`.
		-> Нормализация `phone_number` если указан.
	-> **Подключение к YDB**: Получение credentials через `ydb_creds_from_env()`, создание пулов для `jwt-database` и `firms-database`.
	-> **Транзакция активации в jwt-database** (SerializableReadWrite):
		1. Поиск неактивного пользователя (`is_active = false`) по `email` или `phone_number` в таблице `users`.
		2. Если пользователь не найден или уже активен — rollback и возврат `404 Not Found`.
		3. Проверка `verification_code` на совпадение с переданным `code`.
		4. Проверка срока действия кода (`code_expires_at`) с нормализацией timestamp (поддержка datetime и микросекунд).
		5. Если код неверный или просрочен — rollback и возврат `400 Bad Request`.
		6. Обновление пользователя: `is_active = true`, `verification_code = NULL`, `code_expires_at = NULL`.
		7. Commit транзакции в `jwt-database`.
	-> **Обновление приглашений в firms-database** (отдельная операция с retry):
		-> Поиск записей в `firms-database.Users` с совпадающим `email` и `is_active = false`.
		-> Обновление `user_id` для найденных записей на `user_id` активированного пользователя.
		-> **ВАЖНО**: Не изменяется PK (primary key) — обновляется только для неактивных приглашений.
		-> При ошибке — логирование критической ошибки, но возврат токена (пользователь активирован).
	-> **Генерация JWT-токена**: Использование `issue_jwt` с `user_id`, `JWT_SECRET` и claims включающими `email` и/или `phone_number` пользователя.
	-> **Обработка ошибок**: Логирование всех ошибок с полным traceback, возврат `500 Internal Server Error`.

На выходе:
	-> `200 OK`: {"token": "<jwt_token>"}
	-> `400 Bad Request`: {"message": "Invalid or expired code."} или `{"message": "Email and code are required."}`
	-> `404 Not Found`: `{"message": "User not found or already active."}`
	-> `500 Internal Server Error`: {"message": "Internal Server Error"}

---
#### Зависимости и окружение
- **Необходимые утилиты**: 
	- `utils/` - parse_event, EventParseError, issue_jwt, now_utc, JsonLogger
	- `utils/` - ok, bad_request, not_found, server_error для HTTP-ответов
	- `utils/util_ydb/driver.py` - get_session_pool для подключения к YDB
	- `utils/util_ydb/credentials.py` - ydb_creds_from_env для получения credentials из ENV
- **Переменные окружения**:
	- `SA_KEY_JSON` - JSON ключ сервисного аккаунта (поставляется через Lockbox → ENV)
	- `YDB_ENDPOINT`, `YDB_DATABASE` ([[💾 jwt-database - База данных YandexDatabase]])
	- `YDB_ENDPOINT_FIRMS`, `YDB_DATABASE_FIRMS` ([[💾 firms-database - База данных YandexDatabase]])
	- `JWT_SECRET` - Секретный ключ для генерации JWT токенов 